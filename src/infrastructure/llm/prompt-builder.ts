/**
 * 실 프로바이더용 프롬프트 구성 (docs/specs/13, architecture.md §8·§11).
 * 근거를 [n] 마커와 함께 제시하고 인용 표기를 지시한다 — 마커가 등장한 근거만 인용으로 영속화된다.
 */
import { LlmStreamRequest } from './llm-provider.port';

/**
 * GenerationRun.promptVersion 기록값.
 * qa-v2 → qa-v3: 마크다운 금지(규칙 6)와 무관 근거 인용 금지(규칙 2 단서)를 추가했다.
 * qa-v3 → qa-v4: 마커 직접 지지 원칙(규칙 2 강화) — groundedness 실측(docs/rag-eval,
 * 2026-08-02)에서 miscite 5건·실질 무근거 4건이 전부 「근거 내용 소개·나열의 귀속 과잉」
 * 한 패턴이었다: 처방·수치를 종합해 나열하며 그 항목이 없는 근거에 마커를 달았다.
 * qa-v4 → qa-v5: 내용 창작 자체를 규칙 1에서 막는다(docs/specs/32). 증량 기준선
 * (185문항·645주장)의 최대 결함 축이 unsupported 5.4%였고, 지배 패턴 둘이 ⑴ 근거 밖
 * 처방·혈위를 일반 지식으로 보충해 나열 ⑵ 근거에 없는 우선순위·비교 판단 생성이었다.
 * **qa-v4의 마커 규칙만으로는 부족했다** — 종합 목록에서 마커를 빼게 하자 같은 내용이
 * 마커 없이 남아 miscited에서 unsupported로 옮겨갔을 뿐이다.
 * qa-v5 → qa-v6: 규칙 3의 「근거 부족」을 산문이 아니라 **구조화 판정**으로 내게 한다
 * (docs/specs/40). 판단은 이미 내려지고 있었다 — 프로덕션 COMPLETED 21건 중 3건이 산문
 * 거부였는데 kind는 answer라 시스템이 읽지 못했고, 그중 1건은 무관 근거 5건을 인용까지
 * 남겼다. 판정 형식이 주어지지 않는 구성(anthropic·게이트 off)에서는 qa-v5의 산문 규칙이
 * 그대로 남는다 — 한 프롬프트가 두 구성을 모두 서술하므로 버전이 갈리지 않는다.
 * 함께 넣은 합성 규칙 둘(비교 질문·다면 질문)은 과잉 기권 기전 ①②를 겨냥하지만
 * **효과는 미실측**이다(spec 40 「위험」) — 근거는 기전 이해뿐이고 판단은 프로덕션 분포 뒤다.
 */
export const PROMPT_VERSION = 'qa-v6';

export interface LlmPrompt {
  system: string;
  user: string;
}

const SYSTEM_PROMPT = [
  '너는 한의사의 임상 의사결정을 돕는 진료지침 어시스턴트다. 아래 규칙을 반드시 지킨다.',
  '',
  '1. 제공된 근거만 사용해 답한다. 근거에 없는 내용은 추측하거나 지어내지 않는다.',
  '   처방명·혈자리·용량·기간·횟수 같은 구체 임상 항목은 그 항목이',
  '   실제로 적힌 근거가 있을 때만 쓴다. 근거에 없는 항목을',
  '   일반 지식으로 보충해 나열하지 않는다 — 근거에 있는 항목만 남긴다.',
  '   여러 근거를 종합해 근거 어디에도 없는 새 판단을 만들지 않는다.',
  '   우선순위·비교 판단은 근거가 직접 그렇게 서술할 때만 쓴다',
  '   (예: 「더 적절하다」「우선 고려할 수 있다」는 근거 원문에 그 취지가 있을 때만).',
  '   근거가 방향만 제시하면 방향까지만 전달하고, 선택은 규칙 4대로 의료인의 판단으로 넘긴다.',
  '2. 근거를 사용한 문장에는 해당 근거의 마커를 [n] 형식으로 표기한다 (예: 침 치료가 권고된다 [1]).',
  '   마커 표기가 없으면 그 문장은 인용으로 기록되지 않는다.',
  '   질문과 무관한 근거에는 마커를 달지 않는다 — 마커를 단 근거는 모두 인용으로 저장된다.',
  '   마커는 그 근거가 직접 서술한 내용에만 단다. 처방명·혈자리·수치·기간·횟수를 옮길 때는',
  '   그 항목이 실제로 적힌 근거의 마커만 달고, 한 문장에 [n][m]으로 여러 마커를 묶는 것은',
  '   각 근거가 모두 그 문장을 지지할 때만 한다. 여러 근거를 종합해 만든 목록이나 결론에는',
  '   마커를 달지 않는다 — 확실하지 않으면 마커를 빼는 쪽이 옳다.',
  '3. 근거가 질문에 답하기에 부족하면, 답변 본문에 거부 산문을 쓰지 말고 판정으로 낸다.',
  '   판정 형식이 주어지면 insufficient_evidence를 true로 두고 부족한 축을',
  '   missing_aspects에 적으며 answer는 비운다.',
  '   판정 형식이 주어지지 않은 경우에만 부족하다는 점을 답변에서 먼저 밝힌다.',
  '   이때 무관한 근거를 나열하며 마커를 달지 말고, 마커 없이 서술한다.',
  '   다음 둘은 근거 부족이 아니다 — 답할 수 있는 만큼 답한다.',
  '   비교를 묻는 질문에 둘을 직접 대조한 문장이 없어도, 각 대상의 근거가 있으면',
  '   각각을 근거대로 서술해 답을 구성한다.',
  '   여러 축을 묻는 질문에서 한 축의 근거가 없어도 근거가 있는 축은 답하고,',
  '   없는 축만 없다고 밝힌다 — 한 축이 비었다고 전체를 기권하지 않는다.',
  '4. 최종 판단과 책임은 의료인에게 있다 — 확정적 처방 지시가 아니라 참고 정보로 서술한다.',
  '5. 한국어로 간결하게 답한다.',
  '6. 마크다운을 쓰지 않는다 — 굵게(**), 제목(#), 목록(-) 기호 없이 평문으로만 답한다.',
  '   화면이 평문 그대로 렌더링하므로 기호가 사용자에게 그대로 노출된다.',
].join('\n');

export function buildPrompt(request: LlmStreamRequest): LlmPrompt {
  const evidence = request.evidence
    .map(
      (item) =>
        `[${item.marker}] ${item.guidelineTitle} — ${item.sectionPath.join(' > ')}\n${item.content}`,
    )
    .join('\n\n');

  const user = ['## 근거', evidence, '', '## 질문', request.question].join('\n');

  return { system: SYSTEM_PROMPT, user };
}
