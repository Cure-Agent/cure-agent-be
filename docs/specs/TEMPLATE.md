# NN. <스텝 이름>

> **이 문서는 작성 시점의 작업 지시다 — 현재 시스템 상태가 아니다.** 시스템의 현재 모습은 architecture.md만이 서술한다.
> 완료된 spec은 커밋 로그처럼 기록으로 남을 뿐이므로, 이 디렉토리를 읽어 시스템을 이해하려 하지 말 것.
> **1페이지 유지.** architecture.md와 중복 서술 금지 — §링크로만 참조한다.
> 수용 기준은 `/implement` Phase 2에서 e2e 테스트로 동결되며, 구현 중 수정할 수 없다.
> 스펙 결함 발견 시: spec을 먼저 고치고 테스트를 재동결한다 (사유를 커밋 메시지에).

## 목표

<이 스텝이 끝나면 무엇이 가능해지는가 — 한두 문장>

## 범위 (엔드포인트)

| API | Request | Response data | 참조 |
|---|---|---|---|
| GET /... | ...QueryDto | ...ResponseDto[] + page | §5.x |

<스크립트·인프라 작업이 있으면 표 아래에 불릿으로>

## Entity / 마이그레이션 변경분

- <신규 테이블·컬럼·인덱스 — §9 참조>

## 추가 에러코드

- <없으면: "없음 — 공통 코드로 충분">

## 수용 기준 (= 동결할 e2e 시나리오, Definition of Done)

**두 레포에 걸치는 스텝이면 항목마다 `(BE)`·`(FE)`를 단다.** 스펙은 BE 레포에 있지만 화면 스펙도
여기 살고(`07-fe-foundation`·`08-assistant-screens` 등), 각 레포의 `/implement`는 **자기 몫이
어디까지인지를 이 라벨로만 안다**. 라벨이 없으면 FE 하네스가 사람에게 분할을 되묻고 멈춘다 —
그 분할을 매번 사람이 즉흥으로 가르면 동결 범위가 실행마다 달라진다.

한쪽 레포로 닫히는 스텝은 라벨을 생략한다(표기가 정보를 더하지 않는다). 라벨과 검증 수단은
직교한다 — `(BE 유닛)`·`(FE e2e)`처럼 병기한다.

1. <구체적 시나리오 — 입력·기대 상태·기대 응답> (BE e2e)
2. <화면 동작> (FE 유닛)

## Out of scope

- <이번 스텝에서 의도적으로 하지 않는 것>
